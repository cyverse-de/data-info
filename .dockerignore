# Nothing here reaches the final image -- only the built binary is copied into it -- so this
# exists to keep the build context small and to stop unrelated edits from busting the cache.
.git
.github
docs
k8s
skaffold.yaml
