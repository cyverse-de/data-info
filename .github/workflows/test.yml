name: Run Tests
on:
  push:
    branches:
      - main
      - golang
  pull_request:

jobs:
  test:
    uses: cyverse-de/github-workflows/.github/workflows/go-test.yml@v0.3.1
    with:
      go-version: 1.26
